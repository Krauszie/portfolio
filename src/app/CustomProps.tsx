export interface CustomProps {
  children?: React.ReactNode; // best, accepts everything React can render
}

export interface SocialsStyles {
  containerStyles?: string;
  iconStyles?: string;
}

export interface SwiperIndex {
  activeIndex: number;
}

export interface WorkSliderStyles {
  containerStyles?: string;
  btnStyles?: string;
  iconStyles?: string;
}